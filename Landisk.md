# LanDisk 用户手册

局域网文件快传工具 — 电脑手机在同一 WiFi 下，扫码即连，无需数据线、无需登录、无需安装 App。

---

## 启动与访问

启动 LanDisk 后，服务自动运行在 `http://localhost:22580`。手机扫码（界面右上角 📱）或直接访问电脑的局域网 IP 地址即可打开。

![手机扫码连接](images/05-qr-code.png)

手机和电脑需在同一 WiFi 下。首次启动 Windows 防火墙会弹窗，必须允许 Node.js 访问网络。

---

## 文件浏览

主界面展示当前目录的文件列表，支持多根目录切换、搜索、排序和分页。

![文件浏览主界面](images/01-file-browser.png)

### 多选批量操作

勾选多个文件后，顶部工具栏显示批量操作栏，支持批量下载和批量删除。

![多选批量操作](images/02-batch-select.png)

### 功能说明

| 功能 | 说明 |
|---|---|
| **目录导航** | 点击文件夹进入子目录，面包屑导航可回退 |
| **文件搜索** | 顶部搜索框按文件名过滤 |
| **排序** | 按名称 / 大小 / 时间 / 类型（后缀）排序 |
| **分页** | 每页 5 / 10 / 20 / 50 条 |
| **图标** | 70+ 种文件图标，彩色分类 |

### 操作

| 操作 | 方法 |
|---|---|
| **打开目录** | 点击文件夹行 |
| **打开文件** | 本机点击用系统默认程序打开；其他设备自动下载 |
| **下载文件** | 点击行内「下载」按钮 |
| **删除文件** | 点击行内「删除」按钮，移入回收站 |
| **批量下载** | 勾选多个文件 → 点「批量下载」 |
| **批量删除** | 勾选多个文件 → 点「批量删除」 |

---

## 设置

![设置弹窗](images/03-settings.png)

右上角 ⚙ 打开设置：

- **端口号**：服务监听端口，默认 22580
- **最大上传**：单文件大小上限（MB）
- **显示隐藏文件**：是否在文件列表中显示隐藏文件和系统文件
- **共享目录**：添加/移除文件服务根目录，支持多个根目录

修改配置后点「保存设置」自动持久化到 `%USERPROFILE%\.landisk\config.json`。

---

## 上传文件

### 全局拖拽上传

将文件从桌面或文件夹拖拽到窗口任意位置，出现毛玻璃全屏提示即可松手上传。

![全局拖拽覆盖层](images/06-drag-overlay.png)

### 冲突处理

上传同名文件时弹窗逐项处理：

| 选项 | 效果 |
|---|---|
| **替换** | 覆盖原文件 |
| **保留两份** | 新文件自动加序号（如 `file (1).pdf`）|
| **取消** | 跳过该文件 |

支持「全部替换」/「全部保留」批量操作。

### 安全限制

可执行扩展名（`.exe .bat .cmd .ps1 .sh .msi .dll .sys .vbs .scr`）被阻断上传，写入后自动删除。

---

## 下载

### 单文件下载

点击文件行末的「下载」按钮，浏览器自动下载。

### 批量下载

勾选多个文件 → 点「批量下载」，每个文件依次在新标签页中下载。

---

## 删除

### 回收站优先

删除操作默认调用系统回收站（PowerShell COM 方法），文件可还原。

### 永久删除

回收站不可用时自动回退到永久删除（`fs.rm` / `fs.unlink`），日志中会记录具体去向。

### 批量删除

勾选多个文件 → 点「批量删除」，并行执行，显示百分比进度条。完成后记录每条的实际去向（回收站 / 永久删除）。

---

## 日志查看器

![日志查看器](images/04-log-viewer.png)

右上角 📖 打开日志查看器。日志服务端实时推送（SSE），无需手动刷新。

### 功能

| 功能 | 说明 |
|---|---|
| **SSE 实时推送** | 新日志即时显示，无需轮询 |
| **粘性底部** | 新日志自动滚动到底部；不在底部时显示「回到底部」按钮 |
| **等级过滤** | 全部 / INFO / WARN / ERROR |
| **文本搜索** | 过滤包含指定文本的日志条目 |
| **清除显示** | 仅清除当前查看列表，不影响服务端 |
| **清除本地** | 清空服务端环形缓冲区 + 日志文件 |

### 日志格式

结构化 JSON 格式，统一显示为 `[操作] N 个 → 目录`：

```
[新增] 4 个 → D:\Desktop\kaoyan\test
[新增] 408真题2009.pdf                    (3.5 MB)
[新增] 408真题2009-2025.pdf             (78.4 MB)

[删除] 2 个  D:\Desktop\kaoyan\test → 回收站
[删除] 408真题2009-2025.pdf

[下载] 3 个 → D:\Desktop\kaoyan\test
[下载] report.pdf                        (1.2 MB)
```

文件名字段固定 30 显示宽度，`(大小)` 左对齐。每行操作标记 `[新增]` 除第一行外使用 `visibility: hidden` 隐藏但保留占位，实现自然对齐。

### 服务重启后历史

环形缓冲区上限 200 条，重启后自动从日志文件加载最后 100 条历史，日志查看器不会空白。

---

## 系统托盘

| 操作 | 效果 |
|---|---|
| 关闭窗口 | 隐藏到托盘，后台继续运行 |
| 双击托盘图标 | 显示窗口 |
| 右键托盘 → 开机自启 | 开关系统启动时自动运行 |
| 右键托盘 → 退出 | 完全退出服务 |

单实例运行，多点图标不会重复启动。

---

## 配置

配置文件 `C:\Users\<用户名>\.landisk\config.json`：

```json
{
  "roots": ["D:/Desktop"],
  "port": 22580,
  "maxFileSizeMB": 500,
  "showHiddenFiles": false
}
```

也可通过界面 ⚙ 管理配置和共享目录。

---

## 端口冲突

修改配置文件中的 `port` 字段，或通过设置界面修改后重启服务。

---

## 常见问题

**Q: 手机访问提示「无法连接」？**
A: 检查手机和电脑是否在同一 WiFi，防火墙是否允许 Node.js。

**Q: 上传大文件失败？**
A: 检查设置中的「最大上传」限制，默认 500 MB。

**Q: 安装目录是 Program Files，配置能保存吗？**
A: 配置和数据存于 `%USERPROFILE%\.landisk\`，安装目录只读不影响。

**Q: 日志文件会越来越大吗？**
A: 单个日志文件超过 1 MB 自动轮转归档。旧文件不会自动删除，可手动清理 `%USERPROFILE%\.landisk\logs\`。
