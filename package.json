{
  "name": "landisk",
  "version": "1.0.0",
  "description": "LanDisk - 内网文件服务器",
  "main": "server.js",
  "scripts": {
    "start": "npx tauri dev",
    "server": "node server.js",
    "dev": "node --watch server.js",
    "build": "cd client && npm run build",
    "build:server": "npm --prefix client run build && node scripts/bundle-server.js",
    "tauri": "npx tauri"
  },
  "dependencies": {
    "express": "^4.21.0",
    "jpeg-js": "^0.4.4",
    "multer": "^1.4.5-lts.1",
    "qrcode": "^1.5.4",
    "to-ico": "^1.1.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
